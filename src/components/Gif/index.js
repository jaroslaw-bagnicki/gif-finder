import React, { Component } from 'react';
import styles from './styles.module.scss';

export class Gif extends Component {
  render() {
    return (
      <div> -- Gif component --</div>
    );
  }
}
